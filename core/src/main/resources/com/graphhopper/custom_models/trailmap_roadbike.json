{
  "distance_influence": 15,
  "priority": [
    { "if": "true", "multiply_by": "1.0" },
    { "if": "!bike_access && (!backward_bike_access || roundabout)", "multiply_by": "0" },
    { "else_if": "!bike_access && backward_bike_access", "multiply_by": "0.2" },
    { "if": "road_environment == FERRY", "multiply_by": "0.1" },

    { "if": "gravel_scale == ZERO_MINUS", "multiply_by": "1.0" },
    { "if": "gravel_scale == ZERO", "multiply_by": "0.8" },
    { "if": "gravel_scale == ZERO_PLUS", "multiply_by": "0.7" },
    { "if": "gravel_scale == ONE", "multiply_by": "0.2" },
    { "if": "gravel_scale == TWO", "multiply_by": "0.1" },
    { "if": "gravel_scale == THREE", "multiply_by": "0.05" },
    { "if": "gravel_scale == FOUR", "multiply_by": "0" },
    { "if": "gravel_scale == UNKNOWN", "multiply_by": "0.3" },

    { "if": "predicted_highway == CYCLEWAY", "multiply_by": "1.3" },
    { "if": "predicted_highway == MINOR_ROAD", "multiply_by": "1.1" },
    { "if": "predicted_highway == MAJOR_ROAD", "multiply_by": "0.8" },
    { "if": "predicted_highway == SERVICE_ROAD", "multiply_by": "0.8" },

    { "if": "predicted_highway == GOOD_TRACK", "multiply_by": "0.3" },
    { "if": "predicted_highway == ROUGH_TRACK", "multiply_by": "0" },
    { "if": "predicted_highway == PATH", "multiply_by": "0.1" }
  ],
  "speed": [
    { "if": "true", "limit_to": "28" },
    { "if": "gravel_scale == ZERO", "limit_to": "23" },
    { "if": "gravel_scale == ZERO_PLUS", "limit_to": "19" },
    { "if": "gravel_scale == ONE", "limit_to": "12" },
    { "if": "gravel_scale == TWO", "limit_to": "8" },
    { "if": "gravel_scale == THREE", "limit_to": "6" },
    { "if": "gravel_scale == UNKNOWN", "limit_to": "8" },
    { "if": "gravel_scale == FERRY", "limit_to": "15" },
    { "if": "road_environment == FERRY", "limit_to": "15" },
    { "if": "predicted_highway == FOOTWAY", "limit_to": "4" },
    { "if": "!bike_access && backward_bike_access", "limit_to": "4" }
  ]
}
